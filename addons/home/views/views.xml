<odoo>
  <data>
<!--
    <record model="ir.ui.view" id="home.list">
      <field name="name">home list</field>
      <field name="model">home.home</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->
    <record model="ir.actions.act_window" id="home.action_window">
      <field name="name">Inicio</field>
      <field name="res_model">home.home</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="home.action_server">
      <field name="name">home server</field>
      <field name="model_id" ref="model_home_home"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="home" id="home.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="home.menu_1" parent="home.menu_root"/>
    <menuitem name="Menu 2" id="home.menu_2" parent="home.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="home.menu_1_list" parent="home.menu_1"
              action="home.action_window"/>
    <menuitem name="Server to list" id="home" parent="home.menu_2"
              action="home.action_server"/>
    -->
  </data>
</odoo>